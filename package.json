{
  "name": "@palmshed/sandbox",
  "version": "0.2.0",
  "description": "General-purpose, language-agnostic sandbox runtime with pluggable execution backends",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test dist/test/*.test.js"
  },
  "keywords": [
    "sandbox",
    "security",
    "container",
    "docker",
    "process-isolation",
    "palmshed"
  ],
  "author": "Palmshed",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
