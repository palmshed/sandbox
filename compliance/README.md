# Compliance Suite (`compliance/`)

The compliance suite verifies that SDK implementations and execution backends strictly conform to the runtime specification (`spec/`).

- `sdk/`: SDK behavioral conformance tests.
- `backends/`: Execution engine contract verification.
- `fixtures/`: Shared test fixtures.
