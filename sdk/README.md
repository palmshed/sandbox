# Language SDKs (`sdk/`)

This directory houses language-specific SDK implementations for Palmshed Sandbox:

- `typescript/`: Reference TypeScript / Node.js SDK (Phase 1).
- `rust/`: High-performance Rust SDK & core (Phase 2).
- `go/`: Go SDK (Phase 3).
- `python/`: Python SDK for AI agent frameworks (Phase 3).
